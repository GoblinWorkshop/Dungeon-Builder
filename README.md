# Open-DnD
An open source Dungeon &amp; Dragons toolset for greating epic campaigns and even better maps.

# Install
1. `npm install` (install modules)
2. `npm run dev` (run development server on localhost:8080)
3. `npm run build` (create build)